# Teal Theme

VSCode 主题插件

## 安装

### 方法一：VSIX 文件安装

1. 下载 `teal-theme-0.0.1.vsix`
2. 打开 VSCode，按 `Ctrl+Shift+P`
3. 输入 `Extensions: Install from VSIX...`
4. 选择 `.vsix` 文件
5. 按 `Ctrl+K Ctrl+T` 选择 **Teal Theme** 主题

### 方法二：命令行安装

```bash
code --install-extension teal-theme-0.0.1.vsix
